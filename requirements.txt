# Real-Time Encoding Optimization — Python dependencies
# Compatible with Python 3.9 – 3.12
# NOTE: FFmpeg must be installed separately via system package manager
#   Ubuntu/Debian: sudo apt install ffmpeg -y
#   macOS:         brew install ffmpeg
 
streamlit>=1.30.0
ffmpeg-python>=0.2.0
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
matplotlib>=3.7.0
pandas>=2.0.0
Pillow>=10.0.0
